var proxy = require('express-http-proxy');
var app = require('express')();
//fix ssl localhost
process.env.NODE_TLS_REJECT_UNAUTHORIZED = "0";

app.use('/', proxy('https://localhost:5001'));
app.listen(3000);
# Create a new folder and enter it
mkdir node-proxy-server
cd node-proxy-server

# Initialize a nodejs project
npm init

# Install npm packages
npm install --save express express-http-proxy
# Press enter a bunch when it asks you to enter information. You shouldn't need to change anything.

# Create a javascript file and open it
touch index.js
open index.js

# Paste the code above from kenzouno1 into the file and save it

# Run the proxy server
node index.js
